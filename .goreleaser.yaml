
version: 2

project_name: my-project

before:
  hooks:
    - go mod tidy

builds:

  - id: binary
    main: .
    binary: my-project

    goos:
      - linux
      - windows
      - darwin

    goarch:
      - amd64
      - arm64

archives:
  - id: archive

    formats:
      - tar.gz

    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: checksums.txt

changelog:
  use: github

release:
  draft: false
  prerelease: auto
  footer: |
    Full changelog available in GitHub.

source:
  enabled: true
